export enum ActivityLevel {
    sedetary = 1,
    sparse = 2,
    fair = 3,
    high = 4,
    extreme = 5
}

export enum DietGoal {
    loss = 1,
    lightLoss = 2,
    maintain = 3,
    lightGain = 4,
    gain = 5
}

export enum Sex {
    M = 0,
    F = 1
}